package io.trickle.core;

public enum Controller {
   public static Controller[] $VALUES = new Controller[]{PROXY, TASK, SITEKEYS, SCRIPT, AK_DEVICE, PROXY_RESIDENTIAL, PROXY_CAPTCHA, ACCOUNT, TOKEN, REFERER, LOGIN};
   SCRIPT,
   REFERER,
   PROXY,
   LOGIN,
   ACCOUNT,
   PROXY_RESIDENTIAL,
   TASK,
   PROXY_CAPTCHA,
   TOKEN,
   AK_DEVICE,
   SITEKEYS;
}
