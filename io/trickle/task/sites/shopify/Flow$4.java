package io.trickle.task.sites.shopify;

import io.trickle.task.sites.Site;

public class Flow$4 {
   public static int[] $SwitchMap$io$trickle$task$sites$Site = new int[Site.values().length];

   static {
      try {
         $SwitchMap$io$trickle$task$sites$Site[Site.KITH.ordinal()] = 1;
      } catch (NoSuchFieldError var2) {
      }

      try {
         $SwitchMap$io$trickle$task$sites$Site[Site.MCT.ordinal()] = 2;
      } catch (NoSuchFieldError var1) {
      }

   }
}
