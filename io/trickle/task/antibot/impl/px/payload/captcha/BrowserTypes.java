package io.trickle.task.antibot.impl.px.payload.captcha;

public enum BrowserTypes {
   public static BrowserTypes[] $VALUES = new BrowserTypes[]{ANDROID_WEBVIEW, SAFARI, CHROME, FIREFOX};
   SAFARI,
   CHROME,
   FIREFOX,
   ANDROID_WEBVIEW;
}
