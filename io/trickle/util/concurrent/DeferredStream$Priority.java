package io.trickle.util.concurrent;

public enum DeferredStream$Priority {
   public static DeferredStream$Priority[] $VALUES = new DeferredStream$Priority[]{INSTANT, HIGH, MEDIUM, LOW};
   LOW,
   INSTANT,
   MEDIUM,
   HIGH;

   public static int convert(DeferredStream$Priority var0) {
      // $FF: Couldn't be decompiled
   }
}
